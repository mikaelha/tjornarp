# Copy to .env for local development. These PUBLIC_ vars are exposed to the browser.

# Umami analytics website ID (UUID from cloud.umami.is → Settings → Websites).
# Leave unset to disable the tracking script entirely.
PUBLIC_UMAMI_WEBSITE_ID=
